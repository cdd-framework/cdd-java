package io.github.cddframework;

public enum ScanScope {
    /** Basic hygiene and security headers (15 tests). */
    KERNEL,
    /** Surface exposure and infrastructure (15 tests). */
    TERRITORY
}